import SwiftUI

struct CalendarView: View {
    var body: some View {
        Text("Calendar")
            .font(.largeTitle)
    }
}

struct TimerView: View {
    var body: some View {
        Text("Timer")
            .font(.largeTitle)
    }
}

struct AlarmView: View {
    var body: some View {
        Text("Alarm")
            .font(.largeTitle)
    }
}
